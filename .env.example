# Copier en .env.local et renseigner (laisser vide pour rester 100 % local).
# Projet Supabase -> Settings -> API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Clé publique VAPID pour les notifications Web Push.
# Générer une paire avec : npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=
